apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: whereabouts
  namespace: flux-system
spec:
  interval: 10m
  path: clusters/main/kubernetes/kube-system/whereabouts/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster
